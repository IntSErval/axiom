export default function DashboardPage() {
    return (
        <main className="flex h-screen w-full items-center justify-center bg-[#080810]">
            <div className="rounded-sm border border-white/[0.08] bg-white/[0.03] px-10 py-8 text-center backdrop-blur-xl">
                <p className="font-serif text-2xl font-bold italic text-white/90">
                    Welcome to AXIOM
                </p>
                <p className="mt-2 text-sm text-white/50">
                    Dashboard shell not built yet — this is a placeholder so the
                    swipe-to-enter transition has somewhere to land.
                </p>
            </div>
        </main>
    );
}