// Placeholder route — real handlers added in Phase 2 once Tasks schema lands.
// Returning 501 instead of letting the file compile-empty.
export function GET() {
    return new Response("Not Implemented", { status: 501 });
}
